% nix

# Update nix pkgs
darwin-rebuild switch --flake $HOME/dotfiles/.config/nix#$USER


% dotfiles

# Adopt stow
cd $HOME/dotfiles && stow . --adopt && cd -


% ollama

# Run llama3.2 inline
ollama run llama3.2 "<text>"


% nvim 

# Sync nvim lua to dotfiles
cp -r ~/.config/nvim/lua ~/dotfiles/.config/nvim


% quartz

# Serve 

npx quartz build --serve


# Upload

npx quartz sync
